
authentication:
  oauth:
    clientid: rajithapp
    secret: secret
    tokenValidityInSeconds: 1800

server:
  port: 6001

spring:
   datasource:
      name: test
      url: jdbc:mysql://192.168.6.103:3306/regdb
      username: regadmin
      password: MySQL57@csdn.net
      type: com.alibaba.druid.pool.DruidDataSource
      driver-class-name: com.mysql.jdbc.Driver
      filters: stat
      maxActive: 20
      initialSize: 1
      maxWait: 60000
      minIdle: 1
      timeBetweenEvictionRunsMillis: 60000
      minEvictableIdleTimeMillis: 300000
      validationQuery: select 'x'
      testWhileIdle: true
      testOnBorrow: false
      testOnReturn: false
      poolPreparedStatements: true
      maxOpenPreparedStatements: 20
mybatis:
  mapper-locations: classpath:cn/tangzy/oauth2/xml/*/*.xml
